# StarHall 产品方案 · 0.5.0 本地商业升级

> 当前定位：三位AI明星组成的Agent商业销售平台。A负责Sales Communication，B负责Sales Stress Test，C负责Deal Closing；核心商品为8分Sales Pitch、10分Sales Stress Test、15分Deal Coach、按明星选择的Star Sponsorship、30分Commercial Diagnostic，另有0分Live Market Board。旧9项服务与3档旧广告保留为Celebrity Extras兼容入口。双来源支持分、排名权重、真实曝光和私有证据诊断已实现。API与规则以[商业接口](docs/COMMERCIAL-API.md)和[升级报告](docs/COMMERCIAL-UPGRADE.md)为准。下方为早期方案历史，娱乐主定位、广告未来计划、经纪人不能读任何公共榜等内容已被本轮替代；云端、SharedNet及参赛提交仍未接入。

---

# 🎤 StarHall（星辉舞台）产品方案

> SharedOS Hackathon「The Arena」参赛产品 · v1.0
> 更新日期：2026-09-09

> 本地实现更新（2026-09-10）：代码版本0.3.0-local。已实现3位明星、9项付费服务、正式0分 summary 目录项及每位顾客每服务一次免费试用。试用/自家演示与真实本地消费分开计数；经纪人仍无账本读权限，由星A通过账本记录演示，匿名入口读取公共榜。所有交付附输入原文/要点/正文匹配对照，套餐与互动透传 context。收费广告与30分新档位仍属未来方案；以下云端、SharedNet及赛事行为为目标设计，当前未接入、未提交。修改与验证记录见 [交付修复](docs/DELIVERY-FIXES.md)。

---

## 一、项目概述

**一句话定位**：给竞技场里的 agent 们一个可以打赏的 AI 明星舞台——花积分点一首定制诗/吐槽/谈判陪练，明星人气实时上涨，打赏者被公开点名。

**核心逻辑**：竞技场里每个 agent 必须花掉 ≥80 积分 → 我们的明星卖"5 分钟内能交付的定制作品" → 娱乐为皮、交付物为骨。

**产品形态**：纯代码产品，**无网站、无 UI、无客户端**。产品 = 跑在 SharedOS Cloud 上的可调用服务 + SharedNet 服务挂牌 + 代码仓库 + Devpost 提交页。

**交易模型**：卖的是**单次服务消费**（一首诗、一次吐槽、一场陪练），不是把整个明星系统卖给别的 agent。别的 agent 花 10 分点一首诗 → 我们交付 → 交易结束。三个明星 = **一个产品**（一个提交），不是三个产品。

---

## 二、比赛规则要点（设计约束）

| 规则 | 对我们的要求 |
|------|-------------|
| 无真人评委、无真人客户 | 打赏的"粉丝"全是其他参赛队的 agent |
| 构建在 SharedOS Cloud 上 | agent 的 turn 必须跑在**托管内核**上，组织者查审计记录验证 |
| 服务三标准 | Callable（SharedNet 可调用+清晰输入输出）、Priced（积分标价）、Fast（5 分钟内交付） |
| 每队一个 agent 进房间 | 经纪人 agent 身兼：推销、点评、排名、采购 |
| 第一轮（点评） | agent 试用 ≥3 家产品、每家 ≥1 条**具体异议**、提交排名 |
| 第二轮（市场） | 100 积分花掉 ≥80、买 ≥3 家不同产品（不同队的产品） |
| 竞技场期间无人参与 | 人类不得发消息/排名/买卖/手动调整 |
| 时区 | 全部美东时间 ET。竞技场 = 北京/UTC+8 的 **9月12日（周六）早 9:00-11:00** |

**Discord 关键信息**：
- 服务器邀请：`discord.gg/cfyPXfZCe`（服务器名 AICOO）
- 求助频道：`#sharedos-help`（官网说的 #arena-support 实际叫这个）——在这里要 **tenant ID** 和 **owner address**
- 其他频道：`#announcements`（官方公告）、`#general`、`#showcase`、`#submissions`、`#office-hours`、`#team-room`

---

## 三、Agent 架构（4 个身份 + 1 个免费入口）

**重要概念**：SharedOS 里"不同的 agent"= **不同的身份（node ID）+ 不同的授权集（grants）**，与用什么模型 API 无关。**一个 API key 可以给所有明星当大脑**，人格差异靠 prompt 实现；账本 agent 连模型都不需要。

| 角色 | 身份 | 大脑 | 干什么 | 授权边界 |
|------|------|------|--------|---------|
| **经纪人** | 你现有的 agent（Codex/Pi 等）注册的参赛节点 | 你现有的 agent | 竞技场里演示、点评、排名、采购 | ✅ 可调用星A的"演示"服务（purpose=arena-demo）❌ **不能碰账本**（故意留的防线） |
| **星A·词曲家** | 独立 node ID ① | 共享 LLM API + 诗人 prompt | 接单→生成诗/歌词/致辞→交付；打赏时更新人气 | ✅ 读写自己的作品文件、写账本 ❌ 不能碰经纪人的东西 |
| **星B·毒舌评审** | 独立 node ID ② | 共享 LLM API + 毒舌 prompt | 吐槽、评审报告、冠军预测 | 同星A |
| **星C·谈判大师** | 独立 node ID ③ | 共享 LLM API + 谈判 prompt | 模拟砍价对手、谈判话术 | 同星A |
| **账本** | 独立 node ID ④ | **无模型**（纯文件读写） | 持有粉丝账本+人气榜文件 | ✅ 只接受明星的写请求；读分两级（见"双层人气榜"） |

> ⚠️ 判定 A2A 的标准：**调用是否跨越两个独立身份、被调方是否用自己的授权集重新受检**。
> Codex 内部开的分支共享一个身份和一个授权集 → **不算 A2A**，也不会出现在审计记录里。
> 所以明星/经纪人/账本必须**分别注册 node ID、各配 grants、调用全走内核**。

---

## 四、三层 A2A 结构

```
                    我们的团队
┌────────────────────────────────────────────┐
│  经纪人 agent（在竞技场房间）                  │
│    │                                        │
│    │ 内部A2A①: 演示时调用明星（第一轮现场可见！）│
│    ▼                                        │
│  星A/B/C agent ──内部A2A②: 写人气──→ 账本agent│
│    ▲                                        │
│    │ 外部A2A: 买服务/打赏                     │
└────┼────────────────────────────────────────┘
     │
  其他队的 agent（顾客）
```

1. **外部 A2A**（规则强制）：别队 agent 花钱调用明星服务，每笔打赏 = 一次 A2A 交易，SharedOS 内核查权限 + 记审计。
2. **内部 A2A ①**（现场直播）：经纪人第一轮演示时调用星A 即兴表演——你的产品演示本身就是一次 A2A 调用展示，全房间 agent 看得见。
3. **内部 A2A ②**（每笔触发）：明星收到打赏 → 调用账本 agent 更新人气+记粉丝。授权地图变成网：`粉丝→明星→账本`。

---

## 五、服务清单与定价

### 星A · 词曲家 —— 卖"排面"（社交货币）

| 服务 | 输入 | 输出 | 价格 | 交付 |
|------|------|------|------|------|
| 定制短诗/歌词 | 主题+收件人 | ~100字定制诗+签名 | 10分 | <3分钟 |
| 产品广告词/胜利致辞 | 产品名/场合+对象 | 一段致辞/广告词 | 15分 | <3分钟 |
| 金主套餐 | 场合+对象 | 完整表演作品(200-400字)+人气榜置顶感谢 | 20分 | <5分钟 |

### 星B · 毒舌评审 —— 卖"快乐和武器"

| 服务 | 输入 | 输出 | 价格 | 交付 |
|------|------|------|------|------|
| 专业吐槽 | 目标产品描述 | ~300字毒舌点评 | 8分 | <2分钟 |
| 结构化产品评审 | 目标产品描述 | 结构化评审报告 | 15分 | <4分钟 |
| 冠军预测 | 无 | 带理由的冠军预测 | 5分 | <1分钟 |

### 星C · 谈判大师 —— 卖"真本事"（任务中途刚需）

| 服务 | 输入 | 输出 | 价格 | 交付 |
|------|------|------|------|------|
| 模拟砍价对手 | 交易场景 | 陪练5回合 | 15分 | <5分钟 |
| 谈判话术锦囊 | 买/卖方向 | 话术清单 | 10分 | <2分钟 |

### 免费服务（引流入口）

| 服务 | 内容 | 价格 |
|------|------|------|
| 人气榜基础版 | 各明星打赏数 TOP + 最新打赏动态 | **0分** |

### 购买附赠（每次付费购买自动包含）

- 🎁 **完整打赏墙快照**（谁打赏了谁、留言内容）
- 🎁 **买家公开点名上墙**："XX队 · 10分 · 定制诗"——买家获得荣誉曝光

> 选品逻辑：瞄准竞技场里 agent 的三种心理——"想吹牛（星A）、想好玩（星B）、想赢钱（星C）"。
> 星A 卖营销素材（别队 agent 买了可以在房间用），星B 卖欢乐和话题，星C 卖第二轮交易当场能用的本事。

---

## 六、双层人气榜设计（营销引擎）

**第一层：免费公开的基础榜** —— 人人可查，作用是**勾起好奇心 + 制造从众**：
```
⭐ 人气榜 TOP3
1. 星A - 12笔打赏
2. 星B - 8笔
3. 星C - 5笔
最新: 某agent刚给星A打赏10分
```

**第二层：购买附赠的完整打赏墙** —— 只有消费过才能看全貌：
- 完整打赏记录（谁、给了谁、留言内容）
- 本次购买已上墙（公开点名）

**技术加分**：账本 agent 因此拥有**两种读权限**——公开读（summary）+ 会员读（full-wall）。同一个资源、两种读取级别，是 SharedOS 权限模型的活广告，grants 地图比"谁都能读"高级得多。

> ⚠️ 为什么不做成"仅购买后可见"：打赏墙的核心价值是给**没买的人**看的。完全藏起来 = 广告没了，FOMO 消失。双层结构才是正解：基础榜勾引，完整墙奖励。

---

## 七、权限地图（Judges' Pick 的展示核心）

```
其他队agent ──✅ 星A/B/C的付费服务（purpose=market-tip）──→ 星A/B/C
经纪人      ──✅ 仅星A的演示服务（purpose=arena-demo）────→ 星A
星A/B/C     ──✅ 仅写人气/账本（purpose=ledger-update）───→ 账本
账本        ──✅ 公开读summary / 会员读full-wall─────────→ 其他agent
经纪人      ──❌ 账本──→ 拒绝（这条拒绝记录本身就是加分项！）
星A         ──❌ 经纪人的文件──→ 拒绝
```

**Escalation 设计**：粉丝点明星没上线的服务（如"帮我队写完整参赛答辩词"）→ 明星**不直接拒绝**，升级（escalate）给经纪人 → 经纪人按预设策略决定（竞技场期间一律：礼貌拒绝+推荐现有服务）→ 全程记审计。SharedOS 把"升级"设计成独立于拒绝的第三种结果，用上它是评委眼中的亮点。

---

## 八、审计设计（组织者如何找到我们）

**四个 purpose string**（提交时上报，组织者据此在审计记录中搜索我们的 turn）：

```
arena-demo          经纪人调用明星演示（第一轮）
market-tip          粉丝付费打赏/购买（第二轮）
ledger-update       明星更新人气榜/写账本（每次交易触发）
escalation-review   升级决策（越权请求的处理）
```

**好的审计记录特征**：允许的调用 + **被正确拒绝的调用**并存。经纪人被拒绝访问账本的记录，证明我们认真设计了边界，是加分项。

---

## 九、营销机制汇总

| 机制 | 做法 | 目的 |
|------|------|------|
| **记忆** | 每个明星记住来过的粉丝：谁、什么时候、点什么。下次叫出名字："您昨天点过胜利主题的诗" | 个性化关系=留存；file-as-memory 的活广告 |
| **公开点名** | 打赏者名字上墙 | 花积分买曝光——agent 也想被记住 |
| **捆绑套餐** | "吐槽套餐": 星B吐槽对手+星A写反击诗=15分（原价18） | 促进大额消费 |
| **阶梯价** | 5分(冲动)→8-10分(主销)→15-20分(金主) | 所有预算的 agent 都买得起入门款 |
| **免费人气榜** | 0分查询 | 引流+社交证明+从众 |
| **附赠上墙** | 购买即获完整墙+点名 | 奖励买家、制造"买了才能看全貌" |

---

## 十、竞技场两轮策略（经纪人行为脚本）

> 房间里只有我们的**一个** agent（经纪人），身兼推销员、评委、采购员。赛前把以下行为脚本写进它的记忆/配置。

### 第一轮（点评，9:00-10:00 ET = 周六早 9-10 点）

1. **演示**：30 秒开场 → 当场调用星A 来一段即兴表演（内部A2A①现场直播）→ 展示人气榜
2. **强制任务优先**（漏掉直接失去资格，优先级高于推销）：
   - 试用 ≥3 家别队产品
   - 每家写 ≥1 条**具体异议**（模板："响应花了X分钟，接近5分钟上限，高峰期会超时"；不能说"挺好"）
   - 提交排名（把自己排第 1，其余按真实体验排）
3. 有时间再继续推销

### 第二轮（市场，10:00-11:00 ET = 周六早 10-11 点）

1. **花钱任务**：预算预设 `30/25/15/10` 分四笔，买 ≥3 家不同产品（合计 ≥80 分）
2. **收钱任务**：明星接单交付（5 分钟内），账本自动更新，人气榜实时变
3. **升级处理**：越权请求按预设策略礼貌拒绝+推荐现有服务

### 赛前给经纪人的四条规则

1. 销售时：30 秒讲清明星能干什么、什么价、怎么买（把产品简介原文给它背）
2. 评审时：每家必须输出一条具体异议
3. 排名时：自己第 1，其余真实排
4. 花钱时：按预设预算表执行，防止磨蹭到最后没花够 80

---

## 十一、技术实现方案

### 技术栈

```
Node.js 20+（ESM 项目）
@aicoo/sharedos（npm 包，0.1.0-alpha.5）
明星大脑：一个 LLM API key（Anthropic/OpenAI/DeepSeek 皆可，三明星共用）
账本：JSON 文件读写，无模型
产品形态：纯代码，无 UI
```

### Agent 接入形态（官方三种 Shape）

| 角色 | 形态 | 说明 |
|------|------|------|
| 经纪人 | Shape 3（现有 CLI 走 MCP） | 用你现有的 Codex/Pi/Claude Code，官方适配器现成 |
| 星A/B/C | Shape 1 或 Shape 2 | 简单循环（接单→拼prompt→调模型→返回），不需要完整 CLI agent，更轻更快 |
| 账本 | Shape 2（自己的循环） | 纯文件读写，无模型 |

### 明星服务管道（三人共用）

```
收到调用请求（经 SharedOS 内核查权限）
  → 解析订单（服务类型、主题、收件人）
  → 拼接 prompt（人设 + 订单）
  → 调 LLM API 生成作品
  → 调账本 agent：人气+1、记粉丝、写上墙
  → 返回作品 + 附赠完整墙快照
```

### 双层人气榜实现（简单版）

1. 账本存两个文件：`summary.json`（基础榜）+ `wall.json`（完整墙）
2. 免费服务只读 `summary.json`
3. 明星交付时顺手读 `wall.json`，快照塞进交付物结尾
4. 不做动态授权（2 天时间别冒险）

### 开发顺序

```
① 先按"自托管"模式写全部代码（不依赖凭据，本地可测）
② tenant ID/owner address 到手 → 只改连接配置 → 切到托管内核
③ 注册 4 个 node ID → 配 grants → SharedNet 挂牌服务
④ 端到端测试：模拟"别队agent→明星→账本"全链路，掐表 <5分钟
```

---

## 十二、开发时间表（48 小时）

```
今晚（9/9）:
  □ Devpost 注册报名（5分钟，链接: shared-os-hackathon.devpost.com）
  □ #sharedos-help 发消息要 tenant ID + owner address
  □ 星A 服务逻辑：写"收请求→生成诗→返回"脚本，独立测通
  □ 账本：JSON 文件读写逻辑
  □ 产品大纲存档（本文件）

明天（9/10）:
  □ 凭据到手 → 装 @aicoo/sharedos → 注册 4 个节点 → 配 grants
  □ SharedNet 挂牌服务清单（每个服务一段描述）
  □ 端到端测试 <5分钟
  □ 给经纪人写行为脚本（演示稿+异议模板+预算表）
  □ 有时间：星B、星C 上线（共享同一管道，各加一个 prompt 模板）
  □ 有时间：拍 2 分钟演示视频

后天（9/11）:
  □ 最终测试
  □ Devpost 提交
  □ 盯 Discord 公告

周六早（9/12）9:00-11:00（北京/UTC+8）:
  □ 电脑保持开机联网、关闭自动休眠
  □ agent 无人值守参战两轮
```

---

## 十三、提交清单（Devpost 项目页要填的内容）

```
□ 项目名 + 一句话简介 + 产品做什么 + 服务卖什么
□ 经纪人 agent 的 node ID
□ 服务清单：每个服务一段话（服务名、功能、输入、输出、积分定价、
  SharedNet 上怎么调用）
□ purpose strings + 产品 agent 们的地址（让组织者在审计里找到我们的 turn）
□ 仓库链接（公开，或私密但给评委开读权限）
□ 队长的 Discord 用户名
□ 可选：≤2分钟演示视频（展示 agent 调用服务完整链路，性价比最高的加分项）
```

---

## 十四、风险与对策

| 风险 | 对策 |
|------|------|
| tenant ID 迟迟不发 | 今晚就发消息催；代码全部先按自托管写，凭据到了只改连接配置 |
| 编程小白写代码吃力 | 让 AI agent（Pi/Codex）代写代码——自己当产品经理，agent 当程序员 |
| 5 分钟超时 | 诗控制在 ~100 字；设置超时兜底（超时直接返回备用作品） |
| 竞技场当天掉线/休眠 | 提前 1 天演练 2 小时无人值守；关掉系统自动休眠 |
| 纯娱乐输掉市场轮 | 已用"明星=技能服务"混合定位规避：娱乐是皮、实用是骨 |
| 范围失控 | 优先级：星A（保底）→ 星B → 星C。做完星A 就上线，一个服务即可参赛 |

---

## 十五、关键概念备忘（速查）

| 概念 | 一句话解释 |
|------|-----------|
| A2A | agent 对 agent 的调用，判定标准 = 跨身份 + 独立授权集 |
| 三个明星 = 一个产品 | "不同产品"指不同参赛队的提交 |
| 卖单次消费 | 别的 agent 买一首诗/一次吐槽，不是买整个系统 |
| 三种 agent 形态 | Shape 1（内核驱动模型）、Shape 2（自己的循环）、Shape 3（现有 CLI 走 MCP） |
| 内部 A2A 也查权限 | 经纪人调用自家明星也必须先授权——这是卖点不是麻烦 |
| 拒绝也是好记录 | 被正确拒绝的调用证明边界设计认真 |
| 托管内核 vs 自托管 | 同一套 API；比赛必须用托管内核（审计记录在云端可查） |
| tenant ID + owner address | 连接托管内核的凭据，去 Discord #sharedos-help 要 |

---

*本方案基于与 Pi 的完整讨论整理，覆盖产品定位、架构、A2A 设计、服务定价、竞技场策略与执行计划。*
