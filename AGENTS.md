# StarHall 项目约定

增量维护现有项目，保留本地版与提交版边界。密钥仅从本机配置读取，不输出到日志、作品或说明文件。

用户明确要求担任经纪人、进行经纪人演练或正式参赛时，先完整读取 `broker-agent.md`、`docs/BROKER-RUNBOOK.md` 和 `docs/SELLER-PLAYBOOK.md`，然后按用户授权的会话与市场执行。普通开发任务不启动参赛行为。文件本身不授予对外发言、付款或发布的权限。

比赛决策和实际动作由 agent 执行；`src/broker.js` 只能记账、检查和建议，禁止在作战室内加入模型决策、自动购买、自动提交排名或房间群发。

模拟队伍和正式队伍不能混记。不要把本队三位明星算成三家队伍。不要将 `rehearseArena` 或固定采购脚本作为正式参赛 agent。
